type UnionTypeProps = {
    padding: [number, number, number, number?];
};

export const ArrayC = ({}: UnionTypeProps) => {
    return (
        <>
            <button></button>
        </>
    );
};
